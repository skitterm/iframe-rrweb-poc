{
  "name": "iframe-rrweb-poc",
  "version": "1.0.0",
  "private": true,
  "description": "POC: same-origin parent + iframe child; parent uses rrweb-snapshot snapshot()/rebuild()",
  "type": "module",
  "scripts": {
    "dev:cross-origin": "concurrently -k -n parent,child -c blue,magenta \"VITE_SERVE=parent vite --mode cross-origin\" \"VITE_SERVE=child vite --mode cross-origin\"",
    "dev:same-origin": "vite --mode same-port"
  },
  "keywords": [
    "rrweb",
    "iframe",
    "snapshot"
  ],
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "concurrently": "^9.1.2",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "rrweb-snapshot": "^2.0.0-alpha.4"
  }
}
